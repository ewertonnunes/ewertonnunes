# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional - override model (default: {{ model.default }})
# Use claude-opus-4-7 for higher accuracy at higher cost.
# DEPLOY_AGENT_MODEL={{ model.default }}

# Optional - audit log path (default: /var/log/deploy-agent/audit.jsonl)
# DEPLOY_AGENT_AUDIT_LOG=./audit.jsonl

# Optional - audit actor name (default: deploy-agent)
# DEPLOY_AGENT_ACTOR=ci-deploy-bot
